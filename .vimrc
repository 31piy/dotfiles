call plug#begin('~/.local/share/nvim/plugged')

Plug 'kaicataldo/material.vim'

set number
syntax on
filetype plugin indent on

" show existing tab
set tabstop=2

" when indenting with '>'
set shiftwidth=2

" on pressing tab
set expandtab
